export let <%= routeName%>_route = {
  path: '<%= routeName%>',
  getComponent(nextState:any, cb:any) {
    require.ensure([], (require:any) => {
      cb(undefined, require('./index').<%= cmpName%>);
    });
  },
};